{
  "name": "telegram-ai-bot-vercel",
  "version": "1.0.0",
  "description": "Telegram chatbot that connects to an AI API, hosted on Vercel",
  "main": "api/webhook.js",
  "scripts": {
    "dev": "node api/webhook.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["telegram", "bot", "ai", "vercel"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {},
  "engines": {
    "node": "24.x"
  }
}
